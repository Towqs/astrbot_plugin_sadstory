{
  "cooldown_seconds": {
    "description": "冷却时间（秒）",
    "type": "int",
    "hint": "同一个群两次触发之间的最短间隔，防止刷屏。设为 0 则无冷却。",
    "slider": { "min": 0, "max": 300, "step": 10 },
    "default": 60
  },
  "story_min_messages": {
    "description": "故事最少消息条数",
    "type": "int",
    "hint": "生成故事的最少消息条数，太少故事不完整。",
    "slider": { "min": 10, "max": 60, "step": 5 },
    "default": 30
  },
  "story_max_messages": {
    "description": "故事最多消息条数",
    "type": "int",
    "hint": "生成故事的最多消息条数，太多可能刷屏。",
    "slider": { "min": 20, "max": 120, "step": 5 },
    "default": 80
  },
  "bystander_count": {
    "description": "围观网友数量",
    "type": "int",
    "hint": "故事中穿插评论的围观网友人数。",
    "slider": { "min": 1, "max": 8, "step": 1 },
    "default": 3
  },
  "use_face_emoji": {
    "description": "启用QQ表情",
    "type": "bool",
    "hint": "开启后故事消息中会穿插QQ自带表情（流泪、大哭、叹气等），让聊天更真实。",
    "default": true
  },
  "use_casual_style": {
    "description": "默认使用口语化风格",
    "type": "bool",
    "hint": "当下方「写作风格」没有启用项时的后备。开启=口语化，关闭=文学风格。",
    "default": true
  },
  "prompt_styles": {
    "description": "写作风格",
    "type": "template_list",
    "templates": {
      "style": {
        "name": "写作风格",
        "hint": "每个风格是一套独立的 AI 写作指令，生成故事时从已启用的风格中随机选一个。",
        "items": {
          "style_name": {
            "description": "风格名称",
            "type": "string",
            "hint": "给这个风格起个名字，方便辨认。",
            "default": "新风格"
          },
          "enabled": {
            "description": "启用",
            "type": "bool",
            "hint": "关闭后此风格不会被选中。",
            "default": true
          },
          "prompt_content": {
            "description": "写作指令",
            "type": "text",
            "hint": "告诉 AI 怎么写故事。支持变量：{protagonist}主角名、{bystanders}网友名、{min_msg}最少条数、{max_msg}最多条数、{theme_line}主题、{reference_section}参考模板、{emoji_instruction}表情说明。",
            "default": ""
          }
        }
      }
    }
  },
  "use_story_template": {
    "description": "启用故事模板参考",
    "type": "bool",
    "hint": "开启后生成故事时会随机选一个模板给 AI 参考风格。关闭则纯靠 AI 自由发挥。",
    "default": true
  },
  "story_templates": {
    "description": "故事模板",
    "type": "template_list",
    "templates": {
      "tpl": {
        "name": "故事模板",
        "hint": "给 AI 参考的故事范文，让生成的故事更贴近真实群聊风格。也可以用群指令 /sadstory_addtpl 添加。",
        "items": {
          "tpl_name": {
            "description": "模板名称",
            "type": "string",
            "hint": "给这个模板起个名字。",
            "default": "新模板"
          },
          "enabled": {
            "description": "启用",
            "type": "bool",
            "hint": "关闭后此模板不会被使用。",
            "default": true
          },
          "content": {
            "description": "模板内容",
            "type": "text",
            "hint": "一段完整的故事范文，AI 会参考它的风格来创作新故事。",
            "default": ""
          }
        }
      }
    }
  },
  "source_group_id": {
    "description": "素材群群号",
    "type": "string",
    "hint": "把机器人拉到一个群里，填写该群号，插件会从中读取成员作为故事角色。留空则不从群读取。",
    "default": ""
  },
  "use_card_as_name": {
    "description": "优先使用群名片",
    "type": "bool",
    "hint": "开启后角色昵称优先用群名片，关闭则用QQ昵称。",
    "default": true
  },
  "protagonist_qq_list": {
    "description": "主讲人QQ号",
    "type": "list",
    "hint": "每条填一个QQ号，故事主角从这里随机选。留空则随机选。",
    "default": []
  },
  "bystander_qq_list": {
    "description": "围观网友QQ号",
    "type": "list",
    "hint": "每条填一个QQ号，作为围观网友。留空则从素材群或当前群随机选。",
    "default": []
  },
  "use_virtual_users": {
    "description": "使用虚拟角色",
    "type": "bool",
    "hint": "开启后使用预设虚拟角色（假昵称+灰色头像），适合没有素材群的情况。",
    "default": false
  },
  "chat_provider_id": {
    "description": "指定 LLM 模型",
    "type": "string",
    "hint": "填写 LLM 提供商 ID（如 openai_chat_1），留空则使用默认模型。可在 WebUI「提供商管理」中查看。",
    "default": ""
  }
}