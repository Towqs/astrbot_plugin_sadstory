{
  "source_group_id": {
    "description": "素材群群号",
    "type": "string",
    "hint": "把机器人拉到一个群里，填写该群号，插件会从中读取成员作为故事角色。填 0 或留空则不从群读取。",
    "default": ""
  },
  "use_card_as_name": {
    "description": "优先使用群名片",
    "type": "bool",
    "hint": "开启后优先使用群名片作为角色昵称，关闭则使用QQ昵称。",
    "default": true
  },
  "protagonist_qq_list": {
    "description": "主讲人QQ号",
    "type": "list",
    "hint": "每条填一个QQ号，故事主角会从这里面随机选。昵称自动从群获取。点加号添加。",
    "default": []
  },
  "bystander_qq_list": {
    "description": "网友QQ号",
    "type": "list",
    "hint": "每条填一个QQ号，作为围观网友角色。昵称自动从群获取。点加号添加。留空则从素材群或当前群随机选。",
    "default": []
  },
  "use_virtual_users": {
    "description": "使用虚拟角色",
    "type": "bool",
    "hint": "开启后使用预设虚拟角色（假昵称+灰色头像）。关闭则从上面配置的QQ号或素材群读取真实用户。",
    "default": false
  },
  "cooldown_seconds": {
    "description": "冷却时间（秒）",
    "type": "int",
    "hint": "同一个群两次触发之间的最短间隔，防止刷屏。设为 0 则无冷却。",
    "default": 60
  },
  "story_min_messages": {
    "description": "故事最少消息条数",
    "type": "int",
    "hint": "LLM 生成故事的最少消息条数。",
    "default": 30
  },
  "story_max_messages": {
    "description": "故事最多消息条数",
    "type": "int",
    "hint": "LLM 生成故事的最多消息条数。",
    "default": 80
  },
  "bystander_count": {
    "description": "围观网友数量",
    "type": "int",
    "hint": "故事中穿插评论的围观网友数量。",
    "default": 3
  },
  "chat_provider_id": {
    "description": "LLM 模型",
    "type": "string",
    "hint": "用于生成故事的 LLM 提供商 ID（如 openai_chat_1）。留空则使用 AstrBot 默认模型。可在 WebUI「提供商管理」中查看 ID。",
    "default": ""
  },
  "use_story_template": {
    "description": "启用故事模板参考",
    "type": "bool",
    "hint": "开启后 LLM 生成故事时会使用已启用的模板作为风格参考。关闭则纯靠 LLM 自由发挥。",
    "default": true
  },
  "use_face_emoji": {
    "description": "启用QQ表情",
    "type": "bool",
    "hint": "开启后故事消息中会穿插QQ自带表情（如流泪、大哭、叹气等），让聊天更真实。",
    "default": true
  },
  "story_templates": {
    "description": "故事模板列表",
    "type": "list",
    "hint": "每条格式：模板名|是|模板内容。「是」表示启用，「否」表示禁用。也可通过群内命令管理。",
    "default": []
  }
}
